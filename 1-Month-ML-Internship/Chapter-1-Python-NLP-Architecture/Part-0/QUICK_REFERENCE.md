# Python Quick Reference

Fast lookup for Part-0 concepts

## Data Types
`int, float, str, bool, list, dict, tuple, set`

## String Methods
strip(), lower(), upper(), split(), replace(), find()

## List Methods
append(), remove(), pop(), sort(), reverse()
